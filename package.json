{
  "name": "bc23-core-project-dao",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "dfx": "dfx build dao && dfx deploy dao && dfx build webpage && dfx deploy webpage && dfx generate dao && dfx generate webpage && dfx deploy",
    "webpage": "dfx build webpage && dfx deploy webpage && dfx generate webpage",
    "interface": "dfx deploy interface",
    "build": "vite build",
    "serve": "vite preview"
  },
  "dependencies": {
    "@connect2ic/core": "^0.1.1",
    "@connect2ic/svelte": "^0.1.1",
    "@dfinity/agent": "^0.11.3"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "next",
    "buffer": "^6.0.3",
    "svelte": "^3.42.5",
    "svelte-preprocess": "^4.10.7",
    "vite": "^2.9.13"
  }
}